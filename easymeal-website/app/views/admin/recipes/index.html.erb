<% content_for :page_title do %>
Recettes
<% end %>
<% content_for :page_subtitle do %>
en attente de validation
<% end %>

<div class="row">
    <div class="col-xs-6">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Entrées</h3>
                <div class="box-tools">
                </div>
            </div><!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tbody id="entries">
                        <% if @entries.count == 0 %>
                            <div style="padding: 10px;">Aucune recettes</div>
                        <% else %>
                            <tr>
                                <th>Name</th>
                                <th>Date</th>
                                <th>Show</th>
                            </tr>
                            <% @entries.each do |recipe| %>
                                <tr>
                                    <td><%= recipe.name %></td>
                                    <td><%= recipe.updated_at.strftime("%d/%m/%y") %></td>
                                    <td><%= link_to admin_recipe_path(recipe) do %><i class="fa fa-edit"></i><% end %></td>
                                </tr>
                            <% end %>
                        <% end %>
                </tbody></table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
    
    <div class="col-xs-6">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Go&ucirc;ters</h3>
                <div class="box-tools">
                </div>
            </div><!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tbody id="collations">
                        <% if @collations.count == 0 %>
                            <div style="padding: 10px;">Aucune recettes</div>
                        <% else %>
                            <tr>
                                <th>Name</th>
                                <th>Date</th>
                                <th>Show</th>
                            </tr>
                            <% @desserts.each do |recipe| %>
                                <tr>
                                    <td><%= recipe.name %></td>
                                    <td><%= recipe.updated_at.strftime("%d/%m/%y") %></td>
                                    <td><%= link_to admin_recipe_path(recipe) do %><i class="fa fa-edit"></i><% end %></td>
                                </tr>
                            <% end %>
                        <% end %>
                </tbody></table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
</div>

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Plats</h3>
                <div class="box-tools">
                </div>
            </div><!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tbody id="dishes">
                        <% if @dishes.count == 0 %>
                            <div style="padding: 10px;">Aucune recettes</div>
                        <% else %>
                            <tr>
                                <th>Name</th>
                                <th>Date</th>
                                <th>Show</th>
                            </tr>
                            <% @dishes.each do |recipe| %>
                                <tr>
                                    <td><%= recipe.name %></td>
                                    <td><%= recipe.updated_at.strftime("%d/%m/%y") %></td>
                                    <td><%= link_to admin_recipe_path(recipe) do %><i class="fa fa-edit"></i><% end %></td>
                                </tr>
                            <% end %>
                        <% end %>
                </tbody></table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
</div>

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Desserts</h3>
                <div class="box-tools">
                </div>
            </div><!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover">
                    <tbody id="deserts">
                        <% if @desserts.count == 0 %>
                            <div style="padding: 10px;">Aucune recettes</div>
                        <% else %>
                            <tr>
                                <th>Name</th>
                                <th>Date</th>
                                <th>Show</th>
                            </tr>
                            <% @desserts.each do |recipe| %>
                                <tr>
                                    <td><%= recipe.name %></td>
                                    <td><%= recipe.updated_at.strftime("%d/%m/%y") %></td>
                                    <td><%= link_to admin_recipe_path(recipe) do %><i class="fa fa-edit"></i><% end %></td>
                                </tr>
                            <% end %>
                        <% end %>
                </tbody></table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
</div>

<script>
    
</script>